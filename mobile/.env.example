# Privy — https://dashboard.privy.io → your app → Settings
# The app id is public (it ships in the bundle and only identifies the app).
# Never put the Privy *app secret* in here; that is server-side only.
EXPO_PUBLIC_PRIVY_APP_ID=
EXPO_PUBLIC_PRIVY_CLIENT_ID=
